# MARAHIL store

Local bilingual house site. Payment is a stub (`src/config/payments.js`).

```bash
cd store
npm install
npm run dev
```

Open http://localhost:5173

Preview (after build): `npm run preview` → http://localhost:4173
